CREATE TABLE "Admin" (
    "Id" SERIAL PRIMARY KEY,
    "FirstName" VARCHAR(100) NOT NULL,
    "LastName" VARCHAR(100) NOT NULL,
    "Phone" VARCHAR(15),
    "Email" VARCHAR(150) UNIQUE NOT NULL,
    "PasswordHash" TEXT NOT NULL,
    "CreatedAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


CREATE TABLE sizes (
    id SERIAL PRIMARY KEY,
    size_name VARCHAR(50) NOT NULL,
    description VARCHAR(150),
    is_active BOOLEAN DEFAULT TRUE
);

CREATE TABLE category (
    "Id" SERIAL PRIMARY KEY,
    "Name" VARCHAR(150) NOT NULL,
    "ImageUrl" TEXT, 
    "Status" BOOLEAN DEFAULT TRUE, 
    "CreatedAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE subCategory (
    "Id" SERIAL PRIMARY KEY,
    "CategoryId" INT NOT NULL,
    "SubCategoryName" VARCHAR(150) NOT NULL,
    "subCategoryImageUrl" TEXT,
    "Status" BOOLEAN DEFAULT TRUE,
    "CreatedAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_category
    FOREIGN KEY ("CategoryId") 
    REFERENCES "category"("Id") 
    ON DELETE CASCADE
);

CREATE TABLE childCategory (
    "Id" SERIAL PRIMARY KEY,

    "CategoryId" INT NOT NULL,
    "SubCategoryId" INT NOT NULL,

    "ChildCategoryName" VARCHAR(150) NOT NULL,
    "ChildCategoryImageUrl" TEXT,

    "Status" BOOLEAN DEFAULT TRUE,
    "CreatedAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_child_category_category
        FOREIGN KEY ("CategoryId")
        REFERENCES "category"("Id")
        ON DELETE CASCADE,

    CONSTRAINT fk_child_category_subcategory
        FOREIGN KEY ("SubCategoryId")
        REFERENCES "subcategory"("Id")
        ON DELETE CASCADE
);


CREATE TABLE brand (
    id SERIAL PRIMARY KEY,
    brandname VARCHAR(100) NOT NULL,
    brandimage TEXT,
    isactive BOOLEAN DEFAULT TRUE
);


CREATE TABLE color (
    id SERIAL PRIMARY KEY,
    colorname VARCHAR(100) NOT NULL,
    colorcode VARCHAR(20),
    isactive BOOLEAN DEFAULT TRUE
);


CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    firstname VARCHAR(100) NOT NULL,
    lastname VARCHAR(100),
    email VARCHAR(150) UNIQUE NOT NULL,
    password TEXT NOT NULL,
    role VARCHAR(50) DEFAULT 'USER',
    isactive BOOLEAN DEFAULT TRUE,
    createdat TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE contact (
    id SERIAL PRIMARY KEY,
    fullname VARCHAR(150) NOT NULL,
    email VARCHAR(150) NOT NULL,
    phone VARCHAR(20),
    subject VARCHAR(200),
    message TEXT NOT NULL,
    isread BOOLEAN DEFAULT FALSE,
    createdat TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


CREATE TABLE product (
    id SERIAL PRIMARY KEY,
    productname VARCHAR(200) NOT NULL,
    slug VARCHAR(200) UNIQUE NOT NULL,
    sku VARCHAR(100) UNIQUE NOT NULL,
    shortdescription TEXT,
    description TEXT,
    price DECIMAL(10,2) NOT NULL,
    discountprice DECIMAL(10,2),
    stock INT DEFAULT 0,

    categoryid INT NOT NULL,
    subcategoryid INT NOT NULL,
    childcategoryid INT NOT NULL,
    brandid INT,

    sizeids INT[],
    colorids INT[],
    image TEXT,
    imagegallery TEXT[],

    isactive BOOLEAN DEFAULT TRUE,
    createdat TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_product_category
        FOREIGN KEY (categoryid)
        REFERENCES category("Id")
        ON DELETE CASCADE,

    CONSTRAINT fk_product_subcategory
        FOREIGN KEY (subcategoryid)
        REFERENCES subcategory("Id")
        ON DELETE CASCADE,

    CONSTRAINT fk_product_childcategory
        FOREIGN KEY (childcategoryid)
        REFERENCES childcategory("Id")
        ON DELETE CASCADE,

    CONSTRAINT fk_product_brand
        FOREIGN KEY (brandid)
        REFERENCES brand(id)
        ON DELETE SET NULL
    
);


CREATE TABLE variant (
    id SERIAL PRIMARY KEY,
    variantname VARCHAR(200) NOT NULL,

    productid INT NOT NULL,
    sizeid INT[],
    colorid INT[],

    price DECIMAL(10,2) NOT NULL,
    stock INT DEFAULT 0,

    sku VARCHAR(100),


    image TEXT,          -- main image
    imagegallery TEXT[], -- gallery images

    isactive BOOLEAN DEFAULT TRUE,
    createdat TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_variant_product
        FOREIGN KEY (productid)
        REFERENCES product(id)
        ON DELETE CASCADE,

    CONSTRAINT fk_variant_size
        FOREIGN KEY (sizeid)
        REFERENCES sizes(id),

    CONSTRAINT fk_variant_color
        FOREIGN KEY (colorid)
        REFERENCES color(id),

    CONSTRAINT unique_variant UNIQUE (productid, sizeid, colorid)
);


CREATE TABLE customerlogo (
    id SERIAL PRIMARY KEY,
    customername VARCHAR(255) NOT NULL,
    customerimage TEXT,
    status BOOLEAN DEFAULT TRUE,
    createdat TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE addtocart (
    id SERIAL PRIMARY KEY,
    userid INT NOT NULL,
    productid INT NOT NULL,
    quantity INT NOT NULL DEFAULT 1,
    price NUMERIC(10,2) NOT NULL,
    totalprice NUMERIC(10,2) GENERATED ALWAYS AS (quantity * price) STORED,
    createdat TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    

    -- Important: same product ek user ke liye sirf ek baar
    UNIQUE(userid, productid)
);

CREATE TABLE blogs (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id) ON DELETE CASCADE,
    title VARCHAR(255) NOT NULL,
    slug VARCHAR(255) UNIQUE NOT NULL,
    content TEXT NOT NULL,
    featured_image TEXT,
    status VARCHAR(20) DEFAULT 'draft', -- draft / published
    views INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);



CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    userid INTEGER NOT NULL REFERENCES users(id),
    
    -- Address
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    email VARCHAR(150) NOT NULL,
    mobile VARCHAR(15) NOT NULL,
    pincode VARCHAR(10) NOT NULL,
    address TEXT NOT NULL,
    city VARCHAR(100) NOT NULL,
    state VARCHAR(100) NOT NULL,
    country VARCHAR(100) NOT NULL DEFAULT 'India',
    
    -- Order Info
    total_items INTEGER NOT NULL,
    grand_total DECIMAL(10,2) NOT NULL,
    payment_method VARCHAR(50) NOT NULL DEFAULT 'COD',
    order_status VARCHAR(50) NOT NULL DEFAULT 'pending',
    
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE order_items (
    id SERIAL PRIMARY KEY,
    order_id INTEGER NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
    product_id INTEGER NOT NULL REFERENCES product(id),
    product_name VARCHAR(255) NOT NULL,
    image VARCHAR(500),
    price DECIMAL(10,2) NOT NULL,
    discount_price DECIMAL(10,2),
    quantity INTEGER NOT NULL,
    total_price DECIMAL(10,2) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);










========================================== MicroSite Schema ========================================================= 

CREATE TABLE microsites (
    id BIGSERIAL PRIMARY KEY,

    name VARCHAR(255) NOT NULL,
    slug VARCHAR(255) UNIQUE NOT NULL,

    heading VARCHAR(255),
    content TEXT,

    address TEXT,
    email VARCHAR(255),
    mobile VARCHAR(20),

    logo_image TEXT,
    banner_image TEXT,
    favicon TEXT,

    unique_id UUID DEFAULT gen_random_uuid(),

    start_date DATE,
    end_date DATE,

    status BOOLEAN DEFAULT TRUE,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    url TEXT UNIQUE
);

CREATE TABLE microsite_domains (
    id BIGSERIAL PRIMARY KEY,

    microsite_id BIGINT NOT NULL,
    domain VARCHAR(255) NOT NULL UNIQUE,

    is_primary BOOLEAN DEFAULT FALSE,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_microsite_domain
    FOREIGN KEY (microsite_id)
    REFERENCES microsites(id)
    ON DELETE CASCADE
);

CREATE TABLE microsite_themes (
    id BIGSERIAL PRIMARY KEY,

    microsite_id BIGINT NOT NULL,

    header_color VARCHAR(50),
    text_color VARCHAR(50),
    background_color VARCHAR(50),

    button_color VARCHAR(50),
    button_text_color VARCHAR(50),

    footer_color VARCHAR(50),
    footer_text_color VARCHAR(50),

    font_family VARCHAR(150),

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_microsite_theme
    FOREIGN KEY (microsite_id)
    REFERENCES microsites(id)
    ON DELETE CASCADE
);

CREATE TABLE microsite_seo (
    id BIGSERIAL PRIMARY KEY,

    microsite_id BIGINT NOT NULL,

    meta_title VARCHAR(255),
    meta_description TEXT,
    meta_keywords TEXT,

    og_image TEXT,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_microsite_seo
    FOREIGN KEY (microsite_id)
    REFERENCES microsites(id)
    ON DELETE CASCADE
);

INSERT INTO microsites
(name, slug, heading, content, address, email, mobile, logo_image, banner_image, start_date, end_date)
VALUES
(
'Corporate Gifts Microsite',
'corporate-gifts',
'Welcome to Corporate Gifts',
'Best corporate gifting solution for employees and clients.',
'Delhi, India',
'info@company.com',
'9876543210',
'/uploads/logo.png',
'/uploads/banner.jpg',
'2026-01-01',
'2026-12-31'
);

INSERT INTO microsite_domains (microsite_id, domain, is_primary)
VALUES
(1,'gifts.company.com',TRUE),
(1,'corporategifts.company.com',FALSE),
(1,'gifting.company.com',FALSE);

INSERT INTO microsite_themes
(microsite_id, header_color, text_color, background_color, button_color, button_text_color, footer_color, footer_text_color, font_family)
VALUES
(
1,
'#000000',
'#ffffff',
'#f5f5f5',
'#ff6600',
'#ffffff',
'#111111',
'#cccccc',
'Poppins'
);

INSERT INTO microsite_seo
(microsite_id, meta_title, meta_description, meta_keywords, og_image)
VALUES
(
1,
'Corporate Gifts Microsite',
'Premium corporate gifts for employees and clients.',
'corporate gifts, company gifts, employee gifting',
'/uploads/seo-banner.jpg'
);
